{% extends 'base.html.twig' %}
{% block body %}
    <div id="login">
        <form id="loginForm" method="post" action="{{ path('comprobador') }}">
            <div class="loginData">
                <label for="user">Usuario</label>
                <input type="text" name="user" id="user" class="input-success">
            </div>
            <div class="loginData">
                <label for="password">Password</label>
                <input type="password" name="password" id="password" class="input-danger">
            </div>
            <div id="loginbtn">
                <input type="submit" value="Iniciar Sesión" class="btn btn-success">
                {# <a href="{{ path('registro') }}"> #}
                    <input type="submit" value="No tengo cuenta" class="btn btn-danger" id="cuentaOff">
                {# </a> #}
            </div>
        </form>

    </div>
{% endblock %}

{# {% block javascript %}
    const botonR = document.querySelector("#cuentaOff");
    botonR.addEventListener("click",()=>{
        location.href = "register";
    });
{% endblock %} #}