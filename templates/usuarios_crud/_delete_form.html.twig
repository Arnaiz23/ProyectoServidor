<form method="post" action="{{ path('usuarios_c_r_u_d_delete', {'id': usuario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usuario.id) }}">
    {# <button class="btn">Delete</button> #}
    <button><span class="material-icons-outlined">delete</span></button>
</form>
