<form method="post" action="{{ path('preguntas_delete', {'id': pregunta.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pregunta.id) }}">
    <button><span class="material-icons-outlined">delete</span></button>
</form>
