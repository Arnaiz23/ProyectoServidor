{% extends 'comun/comun.html.twig' %}

{% block claseProducto1 %}activeLink{% endblock %}
{% block claseProducto %}class="activeLink"{% endblock %}

{% block body2 %}

        <div id="central" class="centralTablas">
            <div id="add2">
                <h3>DETALLES</h3>
                <a href="{{ path('productos_new') }}">
                    <button class="btn btn-success" id="administrarUsuario">AÑADIR PRODUCTO</button>
                </a>
            </div>
            <div id="tablaData">
                <table>
                    <tr>
                        <th>ID</th>
                        <th>NOMBRE</th>
                        <th>MARCA</th>
                        <th>TIPO</th>
                        <th>DESCRIPCION CORTA</th>
                        <th>DESCRIPCION LARGA</th>
                        <th>PRECIO</th>
                        <th>DEPORTE</th>
                        <th>DISPONIBILIDAD</th>
                        <th>IMAGEN</th>
                        <th>ELIMINAR</th>
                    </tr>
                    <tr>
                        <td>{{ producto.id }}</td>
                        <td>{{ producto.nombre }}</td>
                        <td>{{ producto.marca }}</td>
                        <td>{{ producto.tipo }}</td>
                        <td>{{ producto.descripcionCorta }}</td>
                        <td>{{ producto.descripcionLarga }}</td>
                        <td>{{ producto.precio }}</td>
                        <td>{{ producto.deporte }}</td>
                        <td>{{ producto.disponibilidad }}</td>
                        <td>{{ producto.imagen }}</td>
                        <td>
                            {{ include('productos/_delete_form.html.twig') }}
                        </td>
                    </tr>
                </table>
            </div>
        </div>
{% endblock %}
